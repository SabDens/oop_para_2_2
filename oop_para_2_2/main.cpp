#include <iostream>
#include "Matrix.h"
#include "contact_stat.h"
int main()
{
	srand(time(0));

	/*Matrix obj2;
	Matrix obj1(3, 3);
	obj1.GetCols();
	obj1.SetCols(10);

	obj1.Init();
	obj1.Print();*/
	Contact_stat number;
	number.Show(); 
	return 0;
}